/*
let password = prompt("What's your password?")
let confirmation = prompt("Re-enter password to confirm")

function comparison(password, confirmation) {
    let approved = password.localeCompare(confirmation)
    return approved
    }

let twoPasswords = comparison(password, confirmation)

if (twoPasswords == 0) {
    console.log("The two passwords are the same")
} else {
    console.log("The two passwords are NOT the same")
}
*/